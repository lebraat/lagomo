describe('Notification Test', () => {
  it('should fail to trigger notification', () => {
    // This test will always fail to trigger our notification system
    throw new Error('Intentional failure to test notification system');
  });
});
